command /menu:
    trigger:
        open chest with 2 rows named "&bGMS Menu" to player
        wait 3 ticks
        format slot 5 of player with arrow named "&7Hub" to run [execute player command "/hub"]