command /menu:
    trigger:
        open gui "LobbySelector" to player
on skript load:
	create a gui with id "LobbySelector" with virtual chest with 1 row named "&6Menu":
		make gui slot 2 with clock named "&bhub":
			teleport player to {spawn} #'on skript load' event doesn't have a 'player', but it will recognize it as it does have.
 
