command /menu:
    trigger:
        open chest inventory with 3 rows named "Menu" to player
        wait 3 ticks
        format slot 5 of player with arrow named "Hub" to run [execute player command "/hub"]